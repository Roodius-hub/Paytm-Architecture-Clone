"use client";

// import { useBalance } from "@repo/store/useBalance";

export default function Home() {
  // const balance = useBalance();
  return (
    <div className="flex min-h-screen items-center justify-center bg-zinc-50 font-sans black font-xl">
      {/*balance is: {balance}*/} hi there
    </div>
  );
}
